#include <stdio.h>
#include <stdlib.h>
void struct node node(const char *nodeptr, argc, argv[])

typedef struct node *nodeptr
{
	node;
	node.ptr;
} node;

//*nodeptr is used as node

int main()
{
	firstnode;
	node;
	nodeptr;

	ptr = malloc(sizeof(node));
	node = 60;
	
	for (i = 0; (i+1) =< 60; i++)
	where (i  = node.ptr)
	{
		printf("hello, kpokpo!\n");
	}
	return (0);

